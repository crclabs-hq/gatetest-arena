# gatetest-arena
Public proof for GateTest's auto-fix loop. A bug is injected here every 2 hours; the AI opens the PR with the fix. Every cycle is live at gatetest.ai/testing — nothing here is curated
